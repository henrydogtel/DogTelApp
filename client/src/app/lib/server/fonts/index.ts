import { Concert_One as ConcertOne, DynaPuff, Indie_Flower as IndieFlower, Neucha } from 'next/font/google';

export const concertOne = ConcertOne({
  subsets: ['latin'],
  weight: '400',
  variable: '--font-ConcertOne',
});

export const dynaPuff = DynaPuff({
  subsets: ['latin'],
  weight: '400',
  variable: '--font-DynaPuff',
});

export const indieFlower = IndieFlower({
  subsets: ['latin'],
  weight: '400',
  variable: '--font-IndieFlower',
});

export const neucha = Neucha({
  subsets: ['latin'],
  weight: '400',
  variable: '--font-Neucha',
});